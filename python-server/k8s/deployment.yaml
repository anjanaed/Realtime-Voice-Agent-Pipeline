apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-server
  template:
    metadata:
      labels:
        app: python-server
    spec:
      containers:
        - name: python-server
          image: local-app:v1
          imagePullPolicy: Never
          ports:
            - containerPort: 8006
          envFrom:
            - secretRef:
                name: python-server-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "0m"
              memory: "0Mi"
            limits:
              cpu: "1000m"
              memory: "1500Mi"
